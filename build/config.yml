version: 0.3.1
info:
  companyName: shad0wP
  productName: Helm
  productIdentifier: io.github.shad0wp.helm
  description: Local AI stack controller (menu bar / system tray)
  copyright: © 2026 shad0wP
  comments: Menu-bar-only controller for a local AI stack
  version: 0.3.0
dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
      - '*_test.go'
    watched_extension:
      - '*.go'
      - '*.js'
      - '*.ts'
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary
fileAssociations: null
other:
  - name: My Other Data
